# v0.0.95 Login Baseline

Historical baseline branch created from tag `android-v0.0.95` for login regression testing only. Application code is unchanged from the tagged commit; only test/CI files were added.
