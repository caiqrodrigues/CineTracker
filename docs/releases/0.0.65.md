# CineTracker Android 0.0.65

Correções após a validação visual da 0.0.64.

- Corrige a causa principal da duplicação visual: `ct53.js` havia recebido acidentalmente a mesma lógica de `ct54.js`; ele volta a ser somente o marcador de build.
- Corrige metadados repetidos/corrompidos nos cards da Watchlist.
- Corrige episódios restantes para `total - assistidos` (ex.: `33/314` → `Faltam 281 episódios`).
- Consolida notas duplicadas nos cards de Home e Assistir.
- Oculta `Carregando perfil...` residual quando o Perfil já está renderizado.
- Preserva estados de `Assistido`, `Acompanhando`, Descobrir estrito e provedores de streaming deduplicados.
- Configurações exibe `CineTracker Android • build 0.0.65`.

Build esperada: `cinetracker-android-0.0.65-debug.apk`.

A versão somente será considerada validada após instalação e teste no aparelho real.
