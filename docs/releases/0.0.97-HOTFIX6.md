# CineTracker Android 0.0.97 HOTFIX 6

P0 startup recovery only. No new features.

## Fixed
- Restores Web login rendering by preserving the base `const media = [...]` block during the auth recovery build.
- Adds executable Web startup smoke testing that requires the login form to render.
- Fixes Android script inlining so JavaScript `$$` tokens are not corrupted by `String.replace` replacement semantics.
- Compiles every inlined Android script before release.
- Keeps the Android runtime self-contained and without automatic Vercel fallback.
- Keeps Supabase auth/session hardening from the previous recovery work.

## Version
- versionName: `0.0.97 HOTFIX 6`
- versionCode: `984`
