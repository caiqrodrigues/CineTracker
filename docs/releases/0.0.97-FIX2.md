# CineTracker 0.0.97 FIX 2

- Corrige definitivamente o login infinito removendo a dependência de `loadCloudState()` e `primeOfficialSuggestions()` do caminho crítico de autenticação.
- O formulário legado de login é substituído e passa a usar um único handler autenticado com timeout real.
- Após sessão válida, o usuário é enviado imediatamente para a Home; sincronização e recomendações carregam em segundo plano com timeout próprio.
- Mantém todos os recursos da v0.0.97 e do primeiro FIX.
- Rodapé e Android exibem `0.0.97 FIX 2`.
- Release separada para evitar confusão com o APK anterior.
