name: Build Android Recovery 0.0.54

on:
  workflow_dispatch:
  push:
    branches: [android-v0.0.54-recovery]
    paths:
      - 'apps/android/**'
      - '.github/workflows/build-android.yml'
      - 'docs/releases/0.0.54.md'

permissions:
  contents: write
  statuses: write

concurrency:
  group: cinetracker-android-054-recovery
  cancel-in-progress: false

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Restore 0.0.49 signing key
        uses: actions/cache@v4
        with:
          path: ~/.cinetracker49/cinetracker.keystore
          key: cinetracker-signing-v3-0.0.49
      - name: Prepare signing key
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ~/.cinetracker49 ~/.android
          if [ ! -f ~/.cinetracker49/cinetracker.keystore ]; then
            keytool -genkeypair -v -keystore ~/.cinetracker49/cinetracker.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=CineTracker Android,O=CineTracker,C=BR"
          fi
          cp ~/.cinetracker49/cinetracker.keystore ~/.android/debug.keystore
      - uses: gradle/actions/setup-gradle@v4
      - name: Validate Android JavaScript
        shell: bash
        run: |
          set -euo pipefail
          for f in ct41.js ct47.js ct48.js ct49.js ct54.js; do node --check "apps/android/app/src/main/assets/$f"; done
      - name: Build APK
        working-directory: apps/android
        run: gradle assembleDebug --stacktrace
      - name: Prepare APK
        run: cp apps/android/app/build/outputs/apk/debug/app-debug.apk cinetracker-android-0.0.54-debug.apk
      - name: Verify package
        shell: bash
        run: |
          set -euo pipefail
          AAPT="$(find "$ANDROID_HOME/build-tools" -type f -name aapt | sort -V | tail -1)"
          PACKAGE="$($AAPT dump badging cinetracker-android-0.0.54-debug.apk | sed -n "s/^package: name='\([^']*\)'.*/\1/p" | head -1)"
          test "$PACKAGE" = "com.cinetracker.app"
      - uses: actions/upload-artifact@v4
        with:
          name: cinetracker-android-0.0.54-debug
          path: cinetracker-android-0.0.54-debug.apk
      - name: Publish Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          gh release view android-v0.0.54 >/dev/null 2>&1 || gh release create android-v0.0.54 --target android-v0.0.54-recovery --title "CineTracker Android 0.0.54 Recovery" --notes-file docs/releases/0.0.54.md
          gh release upload android-v0.0.54 cinetracker-android-0.0.54-debug.apk --clobber
      - name: Mark success
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -fsSL -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }} -d '{"state":"success","context":"Android Build","description":"0.0.54 recovery APK published"}'
      - name: Mark failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -fsSL -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }} -d '{"state":"failure","context":"Android Build","description":"0.0.54 recovery build failed"}'
