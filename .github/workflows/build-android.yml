name: Build Android Legacy (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy pipeline retired
        run: |
          echo "This HOTFIX 5 publisher is intentionally disabled."
          echo "Use the current isolated HOTFIX release workflow instead."
