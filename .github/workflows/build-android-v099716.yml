name: Build Android 0.99.7.16 — r180 Discover/Profile Parity (archived)

on:
  workflow_dispatch:

permissions:
  contents: write
  statuses: write

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android 0.99.7.16 arquivado; use o workflow da versão atual."
