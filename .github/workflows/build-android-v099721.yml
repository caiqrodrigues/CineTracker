name: Build Android 0.99.7.21 — Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android 0.99.7.21 is frozen. Current release line is 0.99.7.22 / r186."
