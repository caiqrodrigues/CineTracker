name: Verify Web 1.0.103 r312 Discover Auth Profile Sports Filters

on:
  push:
    branches: [main, 'fix/r312-discover-auth-profile-sports-filters']
  pull_request:

permissions:
  contents: read

jobs:
  web_1_0_103_r312:
    runs-on: ubuntu-latest
    timeout-minutes: 80
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - name: Version identity r312
        run: |
          set -euo pipefail
          grep -Fq '"version": "1.0.103"' package.json
          grep -Fq '"version": "1.0.103"' apps/web/package.json
          grep -Fq 'build-r312-official.mjs' apps/web/package.json
          test -f apps/web/build-r312.mjs
          test -f apps/web/build-r312-official.mjs
          test -f apps/web/runtime-r312-video-truth.js
          test -f apps/web/test-r312.mjs
          test -f apps/web/test-r312-browser.mjs
      - name: Syntax r312 release chain
        run: |
          set -euo pipefail
          node --check apps/web/runtime-r312-video-truth.js
          node --check apps/web/build-r312.mjs
          node --check apps/web/build-r312-official.mjs
          node --check apps/web/test-r312.mjs
          node --check apps/web/test-r312-browser.mjs
      - name: Regression suite through r312 including latest Discover/Auth/Profile/Sports video failures
        working-directory: apps/web
        run: npm run verify
      - name: Final bundle identity r312 latest-video truth
        working-directory: apps/web
        run: |
          set -euo pipefail
          node build-r312-official.mjs
          node --check dist/app-v312.js
          CT_R312_SKIP_BUILD=1 node test-r312.mjs
          test -f dist/app-v312.js
          test -f dist/app-v312.css
          grep -Fq 'app-v312.js' dist/index.html
          grep -Fq 'app-v312.css' dist/index.html
          node - <<'NODE'
          const r=require('./dist/release.json');
          const ok=r.version==='1.0.103'
            && r.revision==='r312-official-1.0.103'
            && r.auth_proactive_refresh===true
            && r.auth_retry_401_once===true
            && r.discover_public_renderer==='r312-own-card'
            && r.discover_public_exclusion==='seen+watchlist-fail-closed-before-markup'
            && r.discover_public_no_unfiltered_fallback===true
            && r.foryou_layout==='compact-176px-horizontal-slots'
            && r.profile_cached_prepaint===false
            && r.profile_stadium_button===true
            && r.profile_favorite_actors==='canonical-full-payload-after-auth-refresh'
            && r.sports_filter_location==='next+previous-panel-heading'
            && r.sports_jwt_expired_recovery===true
            && r.f1_r311_preserved===true
            && r.android==='1.0.20/10062';
          if(!ok){console.error(r);process.exit(1)}
          NODE

  android_current_source:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - name: Preserve current Android baseline
        run: |
          set -euo pipefail
          grep -Fq "versionName '1.0.20'" apps/android/app/build.gradle
          grep -Fq 'versionCode 10062' apps/android/app/build.gradle

  production_smoke:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [web_1_0_103_r312, android_current_source]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Official production serves and boots 1.0.103 r312
        run: |
          set -euo pipefail
          for i in $(seq 1 300); do
            BODY="$(curl -fsS -H 'Cache-Control: no-cache' "https://mycinetracker.vercel.app/release.json?ct=$(date +%s%N)" || true)"
            if grep -Fq '"version": "1.0.103"' <<<"$BODY" \
              && grep -Fq '"revision": "r312-official-1.0.103"' <<<"$BODY" \
              && grep -Fq '"auth_proactive_refresh": true' <<<"$BODY" \
              && grep -Fq '"discover_public_renderer": "r312-own-card"' <<<"$BODY" \
              && grep -Fq '"discover_public_no_unfiltered_fallback": true' <<<"$BODY" \
              && grep -Fq '"profile_stadium_button": true' <<<"$BODY" \
              && grep -Fq '"sports_filter_location": "next+previous-panel-heading"' <<<"$BODY"; then
              JS="$(curl -fsS -H 'Cache-Control: no-cache' "https://mycinetracker.vercel.app/app-v312.js?ct=$(date +%s%N)" || true)"
              HTML="$(curl -fsS -H 'Cache-Control: no-cache' "https://mycinetracker.vercel.app/?ct=$(date +%s%N)" || true)"
              if grep -Fq "window.__ctR312='jwt-refresh+discover-own-card+foryou-compact+profile-fresh+sport-heading-filters'" <<<"$JS" \
                && grep -Fq 'async function ct312RefreshSession' <<<"$JS" \
                && grep -Fq 'data-ct299-history="stadium"' <<<"$JS" \
                && grep -Fq 'ct312-sport-filter-rail' <<<"$JS" \
                && grep -Fq 'app-v312.js' <<<"$HTML"; then
                DOM="$(google-chrome --headless --no-sandbox --disable-gpu --disable-background-networking --virtual-time-budget=6500 --dump-dom "https://mycinetracker.vercel.app/?ct=$(date +%s%N)" 2>/dev/null || true)"
                if grep -Fq 'CineTracker' <<<"$DOM" && ! grep -Fq '<div id="app"></div>' <<<"$DOM"; then exit 0; fi
              fi
            fi
            sleep 5
          done
          exit 1
