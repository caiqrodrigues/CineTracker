name: Verify

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  web_0997:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - name: Build exact CineTracker Web r164 artifact used by Vercel
        working-directory: apps/web
        run: npm run build
      - name: Assert single r164 authority and requested behavior
        shell: bash
        run: |
          set -euo pipefail
          node --check apps/web/dist/app-v164.js
          test "$(grep -o '<script' apps/web/dist/index.html | wc -l)" -eq 1
          ! grep -Eq 'patch-v[0-9]|hotfix' apps/web/dist/index.html
          test "$(find apps/web/dist -maxdepth 1 -type f -name 'patch-v*.js' | wc -l)" -eq 0
          test ! -f apps/web/dist/app-v162.js
          grep -Fq "single-clean-runtime" apps/web/dist/app-v164.js
          grep -Fq "r164-preload-sports-favorites" apps/web/dist/app-v164.js
          grep -Fq "history_missing_episodes" apps/web/dist/app-v164.js
          grep -Fq "is_caught_up" apps/web/dist/app-v164.js
          grep -Fq "home+history+stats" apps/web/dist/app-v164.js
          grep -Fq "Da sua Watchlist" apps/web/dist/app-v164.js
          grep -Fq "watchlist_movie" apps/web/dist/app-v164.js
          grep -Fq "watchlist_series" apps/web/dist/app-v164.js
          grep -Fq "watchlist_anime" apps/web/dist/app-v164.js
          grep -Fq "data-home-mark-episode" apps/web/dist/app-v164.js
          grep -Fq "Minha Watchlist" apps/web/dist/app-v164.js
          grep -Fq "ESPN + TheSportsDB" apps/web/dist/app-v164.js
          grep -Fq "ct164Prefetch" apps/web/dist/app-v164.js
          grep -Fq "visibilitychange" apps/web/dist/app-v164.js
          grep -Fq "data-ct164-open-favorite" apps/web/dist/app-v164.js
          grep -Fq "cinetracker_sport_favorite_events_v2" apps/web/dist/app-v164.js
          grep -Fq "Ver eventos" apps/web/dist/app-v164.js
          grep -Fq '"revision":"r164-preload-sports-favorites"' apps/web/dist/release.json
          grep -Fq '"outputDirectory": "dist"' apps/web/vercel.json
      - name: Upload exact r164 web artifact
        uses: actions/upload-artifact@v4
        with:
          name: cinetracker-web-r164-dist
          path: apps/web/dist
          if-no-files-found: error
          retention-days: 3

  android_0997_identity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - name: Assert Android 0.99.7.3 identity
        shell: bash
        run: |
          set -euo pipefail
          grep -q "versionName '0.99.7.3'" apps/android/app/build.gradle
          grep -q 'versionCode 9973' apps/android/app/build.gradle
          grep -q 'android-v0.99.7.3-r151-library-sync' scripts/prepare-android-v09973.mjs
          grep -q 'window.__ct0997R151Loaded' scripts/prepare-android-v09973.mjs
          grep -q 'cinetracker:app-foreground' scripts/test-android-v09973.mjs
          grep -q 'android:id="@+id/nav_history"' apps/android/app/src/main/res/layout/activity_main.xml
          grep -q 'android:visibility="gone"' apps/android/app/src/main/res/layout/activity_main.xml
          node --check scripts/prepare-android-v09973.mjs
          node --check scripts/test-android-v09973.mjs

  production_smoke:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [web_0997]
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Production domain serves r164
        shell: bash
        run: |
          set -euo pipefail
          for i in $(seq 1 60); do
            BODY="$(curl -fsS -H 'Cache-Control: no-cache' "https://mycinetracker.vercel.app/release.json?ct=$(date +%s%N)" || true)"
            echo "attempt=$i body=$BODY"
            if grep -Fq '"revision":"r164-preload-sports-favorites"' <<<"$BODY"; then
              JS="$(curl -fsS -H 'Cache-Control: no-cache' "https://mycinetracker.vercel.app/app-v164.js?ct=$(date +%s%N)" || true)"
              if grep -Fq "const REVISION='r164-preload-sports-favorites';" <<<"$JS" && grep -Fq "ct164Prefetch" <<<"$JS" && grep -Fq "cinetracker_sport_favorite_events_v2" <<<"$JS"; then
                echo 'PRODUCTION_R164_OK release+js+preload+favorite-events'
                exit 0
              fi
            fi
            sleep 5
          done
          echo 'Production domain did not serve r164-preload-sports-favorites in time.' >&2
          exit 1
