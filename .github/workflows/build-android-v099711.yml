name: Build Android 0.99.7.11 — archived r177

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived_v099711:
    runs-on: ubuntu-latest
    steps:
      - name: Historical release
        run: echo 'Android 0.99.7.11 is archived. Current Android builds are produced by the latest version workflow.'
