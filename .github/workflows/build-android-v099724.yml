name: Build Android 0.99.7.24 — Login Fix (Frozen)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen_release:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android 0.99.7.24 is frozen; use the current Android release workflow."
