name: Build Android 0.99.7.14 — Immersive Detail + Persistent Nav

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy Android 0.99.7.14 workflow kept for manual historical rebuilds only.'
