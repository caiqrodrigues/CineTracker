name: Build Android 0.99.7.18 — r182 Home/Season Polish

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  android_v099718_historical:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow notice
        run: |
          echo 'Android 0.99.7.18 is a historical release.'
          echo 'For an exact rebuild, check out tag android-v0.99.7.18.'
          echo 'Current main may have newer Android versionCode/versionName.'
