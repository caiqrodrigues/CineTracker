name: Build Android HOTFIX 8

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'ci-status/android-hotfix8-trigger.txt'

permissions:
  contents: write
  statuses: write

concurrency:
  group: cinetracker-android-hotfix8-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android_hotfix8:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Mark HOTFIX 8 build pending
        env: { GH_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        shell: bash
        run: |
          curl -fsSL -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }}" -d '{"state":"pending","context":"Android HOTFIX 8","description":"Building CineTracker Android 0.0.97 HOTFIX 8"}'
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17' }
      - name: Restore dedicated CineTracker signing key
        uses: actions/cache@v4
        with: { path: ~/.cinetracker/cinetracker.keystore, key: cinetracker-signing-v2 }
      - name: Initialize dedicated signing key
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ~/.cinetracker ~/.android
          if [ ! -f ~/.cinetracker/cinetracker.keystore ]; then
            keytool -genkeypair -v -keystore ~/.cinetracker/cinetracker.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=CineTracker Android,O=CineTracker,C=BR"
          fi
          cp ~/.cinetracker/cinetracker.keystore ~/.android/debug.keystore
      - uses: gradle/actions/setup-gradle@v4
      - name: Build Web and prove startup plus accepted post-login in real Chrome
        shell: bash
        run: |
          set -euo pipefail
          npm run build
          node scripts/test-auth-recovery-red.mjs
          npm install --no-save --no-package-lock playwright-core@1.55.0
          CHROME_BIN="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser || true)"
          test -n "$CHROME_BIN"
          export CHROME_BIN
          node scripts/test-real-browser-p0.mjs
          node scripts/test-real-browser-post-login.mjs
          grep -q "window.__ctP0SessionReset = 'hotfix7-once'" dist/index.html
          grep -q 'cinetracker_p0_session_reset_hotfix7' dist/index.html
          grep -q 'window.__ct97ObserverGuard = true' dist/patch-v068-v097-observer-guard.js
          grep -q 'patch-v068-v097-observer-guard.js' dist/index.html
          test "$(grep -bo 'patch-v068-v097-observer-guard.js' dist/index.html | head -1 | cut -d: -f1)" -lt "$(grep -bo 'patch-v068-v097.js' dist/index.html | head -1 | cut -d: -f1)"
          grep -q '0.0.97 HOTFIX 8' dist/patch-v074-hotfix1-version.js
          grep -q 'ct-web-0.0.97-hotfix8' dist/service-worker.js
      - name: Prepare Android runtime and prove embedded post-login in real Chrome
        shell: bash
        run: |
          set -euo pipefail
          node scripts/prepare-android-hotfix2-web.mjs
          node scripts/test-android-inline-hotfix6.mjs
          CINETRACKER_TEST_ROOT=apps/android/app/src/main/assets/hotfix5 node scripts/test-real-browser-post-login.mjs
          grep -q "window.__ctAndroidBundle = 'hotfix8-post-login-inline-authoritative'" apps/android/app/src/main/assets/hotfix5/index.html
          grep -q "window.__ctP0SessionReset = 'hotfix7-once'" apps/android/app/src/main/assets/hotfix5/index.html
          grep -q 'window.__ct97ObserverGuard = true' apps/android/app/src/main/assets/hotfix5/index.html
          grep -q '0.0.97 HOTFIX 8' apps/android/app/src/main/assets/hotfix5/index.html
          grep -q 'const media = \[' apps/android/app/src/main/assets/hotfix5/index.html
          grep -q 'ctLooksLikeJwt' apps/android/app/src/main/assets/hotfix5/index.html
          grep -Fq '$$=(s,r=document)=>' apps/android/app/src/main/assets/hotfix5/index.html
          ! grep -q '<script src="/' apps/android/app/src/main/assets/hotfix5/index.html
          ! grep -q 'patch-v073-v097-fix7.js' apps/android/app/src/main/assets/hotfix5/index.html
          ! grep -q 'auth-preboot-fix7.js' apps/android/app/src/main/assets/hotfix5/index.html
      - name: Build debug HOTFIX 8 APK
        working-directory: apps/android
        shell: bash
        run: gradle assembleDebug --stacktrace
      - name: Verify HOTFIX 8 APK identity, signature and embedded runtime
        shell: bash
        run: |
          set -euo pipefail
          APK="apps/android/app/build/outputs/apk/debug/app-debug.apk"
          OUT="cinetracker-android-0.0.97-HOTFIX8-debug.apk"
          cp "$APK" "$OUT"
          APKSIGNER="$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -1)"
          AAPT="$(find "$ANDROID_HOME/build-tools" -type f -name aapt | sort -V | tail -1)"
          unzip -l "$OUT" | grep -q 'assets/hotfix5/index.html'
          unzip -p "$OUT" assets/hotfix5/index.html | grep -q "window.__ctAndroidBundle = 'hotfix8-post-login-inline-authoritative'"
          unzip -p "$OUT" assets/hotfix5/index.html | grep -q "window.__ctP0SessionReset = 'hotfix7-once'"
          unzip -p "$OUT" assets/hotfix5/index.html | grep -q 'window.__ct97ObserverGuard = true'
          unzip -p "$OUT" assets/hotfix5/index.html | grep -q '0.0.97 HOTFIX 8'
          unzip -p "$OUT" assets/hotfix5/index.html | grep -q 'const media = \['
          unzip -p "$OUT" assets/hotfix5/index.html | grep -Fq '$$=(s,r=document)=>'
          unzip -p "$OUT" assets/hotfix5/index.html | grep -q 'ctLooksLikeJwt'
          "$APKSIGNER" verify --print-certs "$OUT"
          PACKAGE="$($AAPT dump badging "$OUT" | sed -n "s/^package: name='\([^']*\)'.*/\1/p" | head -1)"
          VERSION="$($AAPT dump badging "$OUT" | sed -n "s/^package:.*versionName='\([^']*\)'.*/\1/p" | head -1)"
          VERSION_CODE="$($AAPT dump badging "$OUT" | sed -n "s/^package:.*versionCode='\([^']*\)'.*/\1/p" | head -1)"
          test "$PACKAGE" = "com.cinetracker.app"
          test "$VERSION" = "0.0.97 HOTFIX 8"
          test "$VERSION_CODE" = "986"
          sha256sum "$OUT" | tee hotfix8-sha256.txt
      - uses: actions/upload-artifact@v4
        with:
          name: cinetracker-android-0.0.97-HOTFIX8-debug
          path: |
            cinetracker-android-0.0.97-HOTFIX8-debug.apk
            hotfix8-sha256.txt
          if-no-files-found: error
      - name: Publish Android 0.0.97 HOTFIX 8 release
        if: github.ref == 'refs/heads/main'
        env: { GH_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        shell: bash
        run: |
          set -euo pipefail
          gh release view android-v0.0.97-hotfix8 >/dev/null 2>&1 || gh release create android-v0.0.97-hotfix8 --title "CineTracker Android 0.0.97 HOTFIX 8" --notes-file docs/releases/0.0.97-HOTFIX8.md
          gh release edit android-v0.0.97-hotfix8 --title "CineTracker Android 0.0.97 HOTFIX 8" --notes-file docs/releases/0.0.97-HOTFIX8.md
          gh release upload android-v0.0.97-hotfix8 cinetracker-android-0.0.97-HOTFIX8-debug.apk hotfix8-sha256.txt --clobber
      - name: Mark HOTFIX 8 build success
        if: success()
        env: { GH_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        shell: bash
        run: |
          curl -fsSL -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }}" -d '{"state":"success","context":"Android HOTFIX 8","description":"0.0.97 HOTFIX 8 published"}'
      - name: Mark HOTFIX 8 build failure
        if: failure()
        env: { GH_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        shell: bash
        run: |
          curl -fsSL -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }}" -d '{"state":"failure","context":"Android HOTFIX 8","description":"0.0.97 HOTFIX 8 build failed"}'
