name: Build Android 0.99.7.32 — Historical

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Android 0.99.7.32 is frozen; use the current Android release workflow.'
