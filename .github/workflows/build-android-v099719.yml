name: Build Android 0.99.7.19 — Historical

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  android_v099719_historical:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow notice
        run: |
          echo 'Android 0.99.7.19 is a historical release.'
          echo 'For an exact rebuild, check out tag android-v0.99.7.19.'
          echo 'Current main may have newer Android versionCode/versionName.'
