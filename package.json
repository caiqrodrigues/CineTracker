{
  "name": "cinetracker",
  "private": true,
  "version": "1.0.23",
  "scripts": {
    "build": "cd apps/web && npm run build",
    "verify": "cd apps/web && npm run verify"
  },
  "engines": {
    "node": ">=20"
  }
}
