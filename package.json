{
  "name": "cinetracker",
  "private": true,
  "version": "0.2.6",
  "scripts": {
    "build": "node scripts/verify.mjs && node scripts/build-web.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
