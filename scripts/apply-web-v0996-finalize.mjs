import { readFile, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const root=resolve(process.cwd());
const dirs=[resolve(root,'dist'),resolve(root,'apps/web/dist')];

function replaceRange(source,startMarker,endMarker,replacement,label){
  const start=source.indexOf(startMarker),end=source.indexOf(endMarker,start);
  if(start<0||end<0||end<=start)throw new Error(`0.99.6 finalize: ${label} markers not found`);
  return source.slice(0,start)+replacement+'\n'+source.slice(end);
}

const tmdbCard=`function tmdbCard116(x){const type=x.media_type==='movie'?'movie':'tv',episode=x._calendar_episode||null,episodeCode=episode?\`S\${String(episode.season_number||0).padStart(2,'0')}E\${String(episode.episode_number||0).padStart(2,'0')}\`:null,meta=[episodeCode||year116(x)||null,type==='movie'?'Filme':isAnime116(x)?'Anime':'Série',score116(x)>0?\`★ \${score116(x).toFixed(1)}\`:null].filter(Boolean).join(' · ');return \`<article class="ct116-card" data-ct115-heart-bound="1" data-open-media991="\${type}:\${Number(x.id)}"><button type="button" class="ct116-card-open"><div class="ct116-poster"\${x.poster_path?\` style="background-image:url('\${img116(x.poster_path)}')"\`:''}></div><div class="ct116-body"><b>\${esc116(x.title||x.name||'Sem título')}</b><small>\${esc116(meta)}</small></div></button><button type="button" class="ct116-heart" data-ct116-tmdb-heart="\${type}:\${Number(x.id)}" aria-label="Favoritar">♡</button></article>\`}`;

const fetchDiscover=`async function fetchDiscover116(force=false){
  if(discover116&&!force)return discover116;if(discoverBusy116&&!force)return discoverBusy116;
  discoverBusy116=(async()=>{
    const [dash,ex]=await Promise.all([rpc116('cinetracker_profile_media_dashboard_v0991',{}),rpc116('cinetracker_discovery_exclusions_v0994',{})]);
    if(!Array.isArray(dash)||!ex||!Array.isArray(ex.movie_ids)||!Array.isArray(ex.tv_ids))throw new Error('Exclusões pessoais indisponíveis');
    const block=blocker116(ex),personal=dash.filter(x=>x.is_watchlist&&!hasHistory116(x));
    const personalType=kind=>personal.filter(x=>kind==='movie'?x.media_type==='movie':x.media_type==='tv');
    async function resolvePersonal(kind){
      const local=personalType(kind).map(toTmdb116).filter(x=>Number(x.id)>0).filter(x=>kind==='anime'?isAnime116(x):kind==='tv'?!isAnime116(x):true).sort((a,b)=>score116(b)-score116(a)||year116(b)-year116(a))[0];
      if(local)return local;
      for(const x of personalType(kind).slice(0,24)){
        const q=String(x.title||'').replace(/\\s*\\((?:19|20)\\d{2}\\)\\s*$/,'').trim();if(!q)continue;
        try{const type=x.media_type==='movie'?'movie':'tv',params={query:q,include_adult:false,page:1};if(Number(x.release_year)>0)params[type==='movie'?'year':'first_air_date_year']=Number(x.release_year);const s=await api116(\`/search/\${type}\`,params),want=norm116(q),yr=Number(x.release_year||0);const hits=(s.results||[]).map(v=>({...v,media_type:type})).filter(v=>{const n=norm116(v.title||v.name),y=year116(v);return (n===want||n.includes(want)||want.includes(n))&&(!yr||!y||Math.abs(y-yr)<=1)}).filter(v=>kind==='anime'?isAnime116(v):kind==='tv'?!isAnime116(v):true).sort((a,b)=>Number(b.popularity||0)-Number(a.popularity||0));if(hits[0])return hits[0]}catch{}
      }
      return null;
    }
    const today=new Date();today.setHours(0,0,0,0);const future180=new Date(today);future180.setDate(future180.getDate()+180);const future45=new Date(today);future45.setDate(future45.getDate()+45);const fmt=d=>d.toISOString().slice(0,10),pages=[1,2];
    const jobs=[];
    for(const page of pages){jobs.push(safeApi116('/movie/top_rated',{page}),safeApi116('/tv/top_rated',{page}),safeApi116('/discover/tv',{with_origin_country:'JP',with_genres:'16',sort_by:'vote_average.desc','vote_count.gte':40,include_adult:false,page}),safeApi116('/trending/movie/week',{page}),safeApi116('/trending/tv/week',{page}),safeApi116('/discover/movie',{'primary_release_date.gte':fmt(today),'primary_release_date.lte':fmt(future180),sort_by:'primary_release_date.asc',include_adult:false,page}),safeApi116('/discover/tv',{'first_air_date.gte':fmt(today),'first_air_date.lte':fmt(future180),sort_by:'first_air_date.asc',include_adult:false,page}));}
    const [wm,wt,wa,raw]=await Promise.all([resolvePersonal('movie'),resolvePersonal('tv'),resolvePersonal('anime'),Promise.all(jobs)]);
    const takeKind=(offset,type)=>uniq116(pages.flatMap((_,i)=>(raw[i*7+offset]?.results||[]).map(x=>({...x,media_type:type}))));
    const movieTop=takeKind(0,'movie').filter(x=>year116(x)>1990&&score116(x)>=8&&!block.isBlocked(x));
    const tvTop=takeKind(1,'tv').filter(x=>!isAnime116(x)&&year116(x)>1990&&score116(x)>=7.8&&!block.isBlocked(x));
    const animeTop=takeKind(2,'tv').filter(x=>isAnime116(x)&&year116(x)>1990&&score116(x)>=7.8&&!block.isBlocked(x));
    const trending=uniq116([...takeKind(3,'movie'),...takeKind(4,'tv')]).filter(x=>!block.isBlocked(x)).sort((a,b)=>Number(b.popularity||0)-Number(a.popularity||0)).slice(0,60);
    const upcomingMovies=takeKind(5,'movie').filter(x=>!block.isBlocked(x));
    const upcomingTv=takeKind(6,'tv').filter(x=>!block.isBlocked(x));
    const anticipated=uniq116([...upcomingMovies,...upcomingTv]).sort((a,b)=>String(a.release_date||a.first_air_date||'9999').localeCompare(String(b.release_date||b.first_air_date||'9999'))).slice(0,70);
    const top=uniq116([...movieTop,...tvTop]).sort((a,b)=>score116(b)-score116(a)).slice(0,60);
    const used=new Set(),take=rows=>{const x=(rows||[]).find(v=>!used.has(\`\${v.media_type}:\${Number(v.id)}\`));if(x)used.add(\`\${x.media_type}:\${Number(x.id)}\`);return x||null};
    const daily=take(movieTop),fm=take(movieTop),ft=take(tvTop),fa=take(animeTop);
    const followedIds=[...new Set(dash.filter(x=>x.media_type==='tv'&&(x.is_watchlist||x.is_in_progress||x.is_up_to_date)).map(effectiveId116).filter(x=>x>0))].slice(0,72),tvCalendar=[];
    for(let i=0;i<followedIds.length;i+=8){const details=await Promise.all(followedIds.slice(i,i+8).map(id=>safeApi116(\`/tv/\${id}\`,{})));for(const d of details){const n=d?.next_episode_to_air,air=n?.air_date;if(!d?.id||!air)continue;const dt=new Date(air+'T12:00:00');if(dt<today||dt>future45)continue;tvCalendar.push({...d,media_type:'tv',first_air_date:air,_calendar_episode:{season_number:Number(n.season_number||0),episode_number:Number(n.episode_number||0),name:n.name||''}})}}
    const movieCalendar=upcomingMovies.filter(x=>{const d=x.release_date;if(!d)return false;const dt=new Date(d+'T12:00:00');return dt>=today&&dt<=future45});
    const calendar=uniq116([...movieCalendar,...tvCalendar]).sort((a,b)=>String(a.release_date||a.first_air_date||'9999').localeCompare(String(b.release_date||b.first_air_date||'9999'))).slice(0,80);
    discover116={foryou:{daily,wm,wt,wa,fm,ft,fa},trending,anticipated,top,calendar,generated_at:new Date().toISOString(),strict_exclusions:true};cacheWrite116(DISCOVER_KEY116,discover116);return discover116;
  })().finally(()=>discoverBusy116=null);return discoverBusy116;
}`;

const extras=String.raw`
/* 0.99.6 finalization: targeted poster repair, actor favorites and season-rating carousel. */
const ct116FinalStyle=document.createElement('style');ct116FinalStyle.id='ct0996-final-style';ct116FinalStyle.textContent=`
.ct114-season-body>.ct114-chart{display:none!important}.ct116-cast-heart{position:absolute;top:6px;right:6px;z-index:30;width:30px;height:30px;border:1px solid #7d405a;background:#071018e8;color:#ffa1bd;border-radius:999px;display:grid;place-items:center;cursor:pointer;font-size:17px}.ct116-cast-heart.on,.ct116-person-fav.on{background:#651d3a;border-color:#ff789f;color:#fff}.ct114-person{position:relative!important}.ct116-person-fav{margin-top:10px;border:1px solid #7d405a;background:#211019;color:#ffabc4;border-radius:10px;padding:9px 11px;cursor:pointer}
.ct116-season-ratings{overflow:hidden}.ct116-season-ratings-strip{display:grid;grid-auto-flow:column;grid-auto-columns:min(760px,86vw);gap:12px;overflow-x:auto;scroll-snap-type:x mandatory;padding:2px 1px 10px}.ct116-season-rating-card{scroll-snap-align:start;border:1px solid #26546d;background:radial-gradient(circle at 10% 0,#114c6655,transparent 42%),#07141c;border-radius:14px;padding:12px;min-height:260px}.ct116-season-rating-card h3{margin:0 0 8px;font-size:14px}.ct116-season-rating-note{color:#7893a5;font-size:9px;margin-bottom:8px}.ct116-season-svg-scroll{overflow-x:auto}.ct116-season-rating-card svg{display:block;height:220px}.ct116-season-chart-loading{display:grid;place-items:center;height:220px;color:#7892a4}.ct116-season-chart-empty{display:grid;place-items:center;height:180px;color:#7892a4;border:1px dashed #274a5e;border-radius:10px}
`;document.getElementById(ct116FinalStyle.id)?.remove();document.head.appendChild(ct116FinalStyle);

const posterAttempted116=new Set();let posterBusy116=false;
async function repairVisiblePosters116(){if(posterBusy116)return;const ids=[];for(const card of $$116('[data-card991],[data-ct994-open]')){const p=card.querySelector('.ct116-poster,.ct992-poster,.ct994-poster,.ct991-poster,.ct99-poster');if(!p||String(p.style.backgroundImage||'').includes('url('))continue;const id=Number(card.dataset.card991||card.dataset.ct994Open||0);if(id>0&&!posterAttempted116.has(id)){ids.push(id);if(ids.length>=36)break}}if(!ids.length)return;ids.forEach(x=>posterAttempted116.add(x));posterBusy116=true;try{const headers=typeof authHeaders==='function'?{...authHeaders()}:{},token=(()=>{try{return ctSession?.access_token||''}catch{return''}})();if(token&&!headers.Authorization)headers.Authorization='Bearer '+token;if(!headers.Authorization)return;headers['content-type']='application/json';const r=await fetch(`${SUPABASE_URL}/functions/v1/ct-enrich-media-user?limit=${ids.length}&priority=visible-posters`,{method:'POST',headers,body:JSON.stringify({requested_media_ids:ids})});if(!r.ok)return;const d=await r.json();if(Number(d.ok||0)>0){profile116=null;try{localStorage.removeItem(PROFILE_KEY116)}catch{}if(route116()==='profile'){const fresh=await fetchProfile116(true);if(route116()==='profile')renderProfileData116(fresh)}else window.dispatchEvent(new CustomEvent('cinetracker:data-changed',{detail:{source:'v116-visible-poster-enrichment',count:Number(d.ok||0)}}))}}catch(e){console.warn('[CineTracker 0.99.6] poster repair',e)}finally{posterBusy116=false}}

let actorFav116=null,actorFavBusy116=null;
async function actorFavorites116(force=false){if(actorFav116&&!force)return actorFav116;if(actorFavBusy116&&!force)return actorFavBusy116;actorFavBusy116=sb116('favorite_actors?select=tmdb_person_id,actor_name,profile_path&limit=500').then(rows=>{actorFav116=new Map((rows||[]).map(x=>[Number(x.tmdb_person_id),x]));return actorFav116}).finally(()=>actorFavBusy116=null);return actorFavBusy116}
function actorFavState116(btn,on,label=''){btn.classList.toggle('on',Boolean(on));btn.textContent=on?(label?'♥ Ator favorito':'♥'):(label?'♡ Favoritar ator':'♡');btn.setAttribute('aria-pressed',on?'true':'false')}
async function toggleActorFinal116(id,btn,label=false){await actorFavorites116();let p=actorFav116.get(Number(id));const on=!p;if(on){const d=await api116(`/person/${Number(id)}`),body={tmdb_person_id:Number(id),actor_name:d.name||`TMDB #${id}`,profile_path:d.profile_path||null};await sb116('favorite_actors',{method:'POST',body:JSON.stringify(body)});actorFav116.set(Number(id),body)}else{await sb116(`favorite_actors?tmdb_person_id=eq.${Number(id)}`,{method:'DELETE'});actorFav116.delete(Number(id))}actorFavState116(btn,on,label);profile116=null;try{localStorage.removeItem(PROFILE_KEY116)}catch{}window.dispatchEvent(new CustomEvent('cinetracker:data-changed',{detail:{source:'v116-actor-favorite',tmdb_person_id:Number(id),favorite:on}}))}
async function decorateCastFinal116(){const o=$116('#ct114-overlay');if(!o)return;await actorFavorites116().catch(()=>new Map());for(const card of $$116('[data-ct114-person]',o)){const id=Number(card.dataset.ct114Person);if(!id)continue;card.onclick=e=>{if(e.target.closest('.ct116-cast-heart'))return;e.preventDefault();e.stopPropagation();void window.__ct0994OpenPerson?.(id)};let b=card.querySelector('.ct116-cast-heart');if(!b){b=document.createElement('button');b.type='button';b.className='ct116-cast-heart';b.onclick=e=>{e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();void toggleActorFinal116(id,b,false)};card.appendChild(b)}actorFavState116(b,actorFav116.has(id),false)}}
async function decoratePersonFavoriteFinal116(id){for(const delay of [0,90,260])setTimeout(async()=>{const o=$116('#ct114-overlay'),hero=$116('.ct114-person-hero',o);if(!o||!hero)return;await actorFavorites116().catch(()=>new Map());let b=$116('.ct116-person-fav',hero);if(!b){b=document.createElement('button');b.type='button';b.className='ct116-person-fav';(hero.children[1]||hero).appendChild(b);b.onclick=e=>{e.preventDefault();e.stopPropagation();void toggleActorFinal116(Number(id),b,true)}}actorFavState116(b,actorFav116.has(Number(id)),true)},delay)}

function seasonChartSvg116(seasonNo,episodes){const rated=(episodes||[]).filter(e=>Number(e.vote_average)>0);if(!rated.length)return '<div class="ct116-season-chart-empty">Ainda não há avaliações suficientes no TMDB.</div>';const vals=rated.map(e=>Number(e.vote_average)),min=Math.min(...vals),max=Math.max(...vals),W=Math.max(620,rated.length*52),H=220,left=34,right=18,top=15,bottom=42,iw=W-left-right,ih=H-top-bottom,x=i=>left+(rated.length===1?iw/2:(i/(rated.length-1))*iw),y=v=>top+((10-v)/10)*ih,points=rated.map((e,i)=>`${x(i)},${y(Number(e.vote_average))}`).join(' '),grid=[0,2,4,6,8,10].map(v=>`<line x1="${left}" y1="${y(v)}" x2="${W-right}" y2="${y(v)}" stroke="#15394b" stroke-width="1"/><text x="4" y="${y(v)+3}" fill="#718d9d" font-size="9">${v}</text>`).join(''),nodes=rated.map((e,i)=>{const v=Number(e.vote_average),best=v===max,worst=v===min,fill=best?'#48e39a':worst?'#ff5f59':'#58cfff',code=`S${String(seasonNo).padStart(2,'0')}E${String(e.episode_number).padStart(2,'0')}`,votes=Number(e.vote_count||0);return `<circle cx="${x(i)}" cy="${y(v)}" r="${best||worst?5:4}" fill="${fill}" stroke="#061018" stroke-width="2"><title>${code} · ${v.toFixed(1)} · ${esc116(e.name||'')} · ${votes} votos</title></circle><text x="${x(i)}" y="${H-10}" fill="#7894a5" font-size="8" text-anchor="middle">${code}</text>`}).join('');return `<div class="ct116-season-rating-note">Melhor episódio em verde · pior em vermelho · escala 0–10</div><div class="ct116-season-svg-scroll"><svg width="${W}" viewBox="0 0 ${W} ${H}" role="img" aria-label="Avaliações da temporada ${seasonNo}">${grid}<polyline points="${points}" fill="none" stroke="#55cfff" stroke-width="2.3" stroke-linejoin="round" stroke-linecap="round"/>${nodes}</svg></div>`}
async function loadSeasonRating116(tmdbId,card){if(!card||card.dataset.loaded==='1'||card.dataset.loading==='1')return;card.dataset.loading='1';const n=Number(card.dataset.ct116SeasonRating);try{const d=await api116(`/tv/${Number(tmdbId)}/season/${n}`);card.querySelector('.ct116-season-rating-content').innerHTML=seasonChartSvg116(n,(d.episodes||[]).filter(e=>Number(e.episode_number)>0));card.dataset.loaded='1'}catch{card.querySelector('.ct116-season-rating-content').innerHTML='<div class="ct116-season-chart-empty">Falha ao carregar avaliações.</div>'}finally{delete card.dataset.loading}}
async function decorateSeasonRatings116(tmdbId){const o=$116('#ct114-overlay'),body=$116('.ct114-body',o);if(!o||!body||!Number(tmdbId))return;let sec=$116('#ct116-season-ratings',o);if(sec)return;let detail;try{detail=await api116(`/tv/${Number(tmdbId)}`)}catch{return}const seasons=(detail.seasons||[]).filter(s=>Number(s.season_number)>0&&Number(s.episode_count)>0);if(!seasons.length)return;sec=document.createElement('section');sec.id='ct116-season-ratings';sec.className='ct114-section ct116-season-ratings';sec.innerHTML=`<h2>Avaliações dos episódios por temporada</h2><div class="ct116-season-ratings-strip">${seasons.map(s=>`<article class="ct116-season-rating-card" data-ct116-season-rating="${Number(s.season_number)}"><h3>Temporada ${Number(s.season_number)}</h3><div class="ct116-season-rating-content"><div class="ct116-season-chart-loading">Carregando avaliações…</div></div></article>`).join('')}</div>`;const episodeSec=body.querySelector('.ct114-section');episodeSec?.insertAdjacentElement('afterend',sec);const strip=sec.querySelector('.ct116-season-ratings-strip'),cards=[...sec.querySelectorAll('[data-ct116-season-rating]')],open=Number(o.querySelector('.ct114-season.open')?.dataset.ct114Season||seasons[0].season_number),idx=Math.max(0,cards.findIndex(c=>Number(c.dataset.ct116SeasonRating)===open));const loadNear=()=>{const r=strip.getBoundingClientRect();for(const c of cards){const cr=c.getBoundingClientRect();if(cr.right>=r.left-r.width&&cr.left<=r.right+r.width)void loadSeasonRating116(tmdbId,c)}};requestAnimationFrame(()=>{cards[idx]?.scrollIntoView({behavior:'auto',block:'nearest',inline:'center'});loadNear()});let timer;strip.addEventListener('scroll',()=>{clearTimeout(timer);timer=setTimeout(loadNear,80)},{passive:true});for(const i of [idx-1,idx,idx+1])if(cards[i])void loadSeasonRating116(tmdbId,cards[i])}

const finalOpenDetail116=window.__ct0994OpenDetail;if(typeof finalOpenDetail116==='function'){window.__ct0994OpenDetail=async function(type,id){const r=await finalOpenDetail116(type,id);for(const d of [0,100,320])setTimeout(()=>void decorateCastFinal116(),d);if(type==='tv')for(const d of [40,180])setTimeout(()=>void decorateSeasonRatings116(Number(id)),d);return r};window.ct91OpenMedia=(type,id)=>window.__ct0994OpenDetail(type,id);window.ct92OpenMedia=(type,id)=>window.__ct0994OpenDetail(type,id)}
const finalOpenById116=window.__ct0994OpenMediaById;if(typeof finalOpenById116==='function'){window.__ct0994OpenMediaById=async function(mediaId){const rows=await sb116(`media?select=media_type,tmdb_id,raw_tmdb&id=eq.${Number(mediaId)}&limit=1`).catch(()=>[]),m=rows?.[0],tid=Number(m?.tmdb_id)>0?Number(m.tmdb_id):Number(m?.raw_tmdb?.source_tmdb_id||0);const r=await finalOpenById116(mediaId);for(const d of [0,100,320])setTimeout(()=>void decorateCastFinal116(),d);if(m?.media_type==='tv'&&tid>0)for(const d of [40,180])setTimeout(()=>void decorateSeasonRatings116(tid),d);return r}}
const finalOpenPerson116=window.__ct0994OpenPerson;if(typeof finalOpenPerson116==='function'){window.__ct0994OpenPerson=async function(id){const r=await finalOpenPerson116(id);void decoratePersonFavoriteFinal116(Number(id));return r}}

const finalNavigate116=window.__ct0994Navigate;if(typeof finalNavigate116==='function'&&!finalNavigate116.__ct116FinalWrapped){const fn=async function(target){const r=await finalNavigate116.apply(this,arguments);for(const d of [120,650,1800])setTimeout(()=>void repairVisiblePosters116(),d);return r};fn.__ct116FinalWrapped=true;window.__ct0994Navigate=fn;window.ct0994Navigate=fn;window.ct991Navigate=fn;window.ct0992Navigate=fn;window.ct99Navigate=fn;window.ct98Navigate=fn}
for(const d of [350,1200,2600])setTimeout(()=>void repairVisiblePosters116(),d);
`;

for(const dir of dirs){
  const p=resolve(dir,'patch-v116-v0996-authoritative.js');let src=await readFile(p,'utf8');
  src=src.replace("const PROFILE_KEY116='ct0996_profile_snapshot_v1';","const PROFILE_KEY116='ct0996_profile_snapshot_v2';").replace("const DISCOVER_KEY116='ct0996_discover_snapshot_v1';","const DISCOVER_KEY116='ct0996_discover_snapshot_v2';");
  src=replaceRange(src,'function tmdbCard116(x){','async function ensureMedia116',tmdbCard,'calendar episode card');
  src=replaceRange(src,'async function fetchDiscover116(force=false){','function discoverTabs116()',fetchDiscover,'strict discover');
  if(!src.includes('0.99.6 finalization: targeted poster repair')){const end=src.lastIndexOf('\n})();');if(end<0)throw new Error('0.99.6 finalize: IIFE end not found');src=src.slice(0,end)+'\n'+extras+'\n'+src.slice(end)}
  await writeFile(p,src,'utf8');
}
console.log('CineTracker 0.99.6 final: posters, actors, profile activity, season chart carousel and strict Discover emitted.');
