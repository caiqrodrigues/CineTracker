import {readFile} from 'node:fs/promises';import {resolve,extname} from 'node:path';import {createServer} from 'node:http';import {spawn,execFileSync} from 'node:child_process';
await import('./build-r361.mjs');
let bin='';for(const x of ['google-chrome','chromium','chromium-browser']){try{execFileSync('which',[x],{stdio:'ignore'});bin=x;break}catch{}}
if(!bin)throw new Error('Chromium unavailable');execFileSync(process.execPath,['--check',resolve('dist/app-v361.js')],{stdio:'inherit'});
const dist=resolve('dist'),baseRaw=await readFile(resolve(dist,'index.html'),'utf8');
const bridge=`<script>window.__ctR340TestBridge={load:async()=>({ok:true}),sync:async()=>({ok:true})};window.__ctDiagErrors=[];window.addEventListener('error',e=>window.__ctDiagErrors.push(String(e.error?.stack||e.message||e)));window.addEventListener('unhandledrejection',e=>window.__ctDiagErrors.push('PROMISE:'+String(e.reason?.stack||e.reason||e)))</script>`;
const probe=`<script>setTimeout(async()=>{try{
 const ok=(v,m)=>{if(!v)throw new Error(m)},sleep=ms=>new Promise(r=>setTimeout(r,ms));
 history.replaceState({},'','/discover');document.querySelector('#app').innerHTML='<div data-ct319-discover><div data-ct319-content></div></div>';
 const item=(id,type,title,g=[])=>({tmdb_id:id,id,media_type:type,title,name:title,poster_path:null,vote_average:8,release_date:'2026-09-01',first_air_date:'2026-09-01',genre_ids:g});
 const mk=(base,type,prefix,g=[])=>Array.from({length:7},(_,i)=>({...item(base+i,type,prefix+(i+1),g),...(g.includes(16)?{original_language:'ja',origin_country:['JP']}:{} )}));
 const wm=mk(101,'movie','WM'),ws=mk(201,'tv','WS'),wa=mk(301,'tv','WA',[16]),fm=mk(401,'movie','FM'),fs=mk(501,'tv','FS'),fa=mk(601,'tv','FA',[16]),daily=mk(701,'movie','D');
 window.__ctR309Test.setForYouState({watchPools:{movie:wm,series:ws,anime:wa},freshPools:{movie:fm,series:fs,anime:fa},watchIndex:{movie:0,series:0,anime:0},freshIndex:{movie:0,series:0,anime:0},dailyPool:daily,dailyIndex:0,complete:true,initial:{watch:{movie:wm[0],series:ws[0],anime:wa[0]},fresh:{movie:fm[0],series:fs[0],anime:fa[0]},daily:daily[0]}});
 const calls={watchlist:[],seen:[],swap:[]};window.__ctR360.setTestBridge({watchlist:async(t,id)=>{calls.watchlist.push(t+':'+id);await sleep(15);return true},seen:async(t,id)=>{calls.seen.push(t+':'+id);await sleep(15);return true},swapMemory:async(t,id,s)=>{calls.swap.push(t+':'+id+':'+s);return true}});
 ok(window.__ctR336.paintForYou(),'paint failed');window.__ctR361.armAll();window.__ctR361.bind();await sleep(60);
 const key=n=>document.querySelector('[data-ct336-slot="'+n+'"] [data-ct288-card]')?.dataset?.ct288Card||'';
 function click(sel){const b=document.querySelector(sel);ok(b,'missing '+sel);ok(!b.disabled,'disabled before click '+sel);b.scrollIntoView({block:'center',inline:'center',behavior:'auto'});const r=b.getBoundingClientRect(),x=r.left+r.width/2,y=r.top+r.height/2,hit=document.elementFromPoint(x,y);ok(hit&&(hit===b||b.contains(hit)),'not hit-testable '+sel+' hit='+(hit?.className||hit?.tagName||'none'));hit.dispatchEvent(new MouseEvent('click',{bubbles:true,cancelable:true,view:window,clientX:x,clientY:y}));}
 async function repeatSwap(name,n){const sel='[data-ct336-slot="'+name+'"] [data-ct336-swap-only]';for(let i=0;i<n;i++){const before=key(name);click(sel);const after=key(name);ok(after&&after!==before,name+' swap '+i+' failed '+before+' => '+after);await sleep(25);const b=document.querySelector(sel);ok(b&&!b.disabled,name+' swap disabled after repaint '+i);ok(getComputedStyle(b).pointerEvents!=='none',name+' pointer dead '+i);}}
 await repeatSwap('fresh:movie',5);await repeatSwap('watch:movie',4);await repeatSwap('daily',4);
 for(let i=0;i<2;i++){const n='fresh:series',sel='[data-ct336-slot="'+n+'"] [data-ct336-action="watchlist"]',before=key(n);click(sel);const after=key(n);ok(after&&after!==before,'Watchlist repeat '+i+' failed');await sleep(25);ok(!document.querySelector(sel).disabled,'Watchlist disabled after repaint')}
 for(let i=0;i<2;i++){const n='watch:series',sel='[data-ct336-slot="'+n+'"] [data-ct336-action="seen"]',before=key(n);click(sel);const after=key(n);ok(after&&after!==before,'Seen repeat '+i+' failed');await sleep(25);ok(!document.querySelector(sel).disabled,'Seen disabled after repaint')}
 ok(calls.watchlist.length===2,'watchlist persistence count '+calls.watchlist.length);ok(calls.seen.length===2,'seen persistence count '+calls.seen.length);ok(calls.swap.length>=13,'swap memory count '+calls.swap.length);
 await sleep(140);for(const b of document.querySelectorAll('[data-ct336-foryou] .ct336-actions button:not([disabled])'))ok(getComputedStyle(b).pointerEvents!=='none','final pointer dead');
 document.documentElement.dataset.ct361done='1';
}catch(e){document.documentElement.dataset.ct361probe='fail:'+String(e?.stack||e)+' ERRORS='+(window.__ctDiagErrors||[]).join(' || ')}},1700)</script>`;
const html=baseRaw.replace('</head>',bridge+'</head>').replace('</body>',probe+'</body>'),mime={'.js':'text/javascript','.css':'text/css','.json':'application/json','.html':'text/html'};
const server=createServer(async(req,res)=>{try{const u=new URL(req.url||'/','http://127.0.0.1'),p=u.pathname;if(p==='/'||p==='/discover'){res.writeHead(200,{'content-type':'text/html','cache-control':'no-store'});res.end(html);return}const file=resolve(dist,p.startsWith('/')?p.slice(1):p);if(!file.startsWith(dist)){res.writeHead(403);res.end();return}const body=await readFile(file);res.writeHead(200,{'content-type':mime[extname(file)]||'application/octet-stream','cache-control':'no-store'});res.end(body)}catch{res.writeHead(404);res.end('not found')}});
await new Promise(r=>server.listen(0,'127.0.0.1',r));const port=server.address().port;const child=spawn(bin,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage','--disable-background-networking','--window-size=1664,936','--virtual-time-budget=18000','--dump-dom','http://127.0.0.1:'+port+'/'],{stdio:['ignore','pipe','pipe']});let out='',err='';child.stdout.on('data',d=>out+=d);child.stderr.on('data',d=>err+=d);const code=await new Promise(r=>child.on('close',r));await new Promise(r=>server.close(r));if(code!==0)throw new Error('Chromium '+code+' '+err.slice(-1200));if(!/data-ct361done="1"/.test(out)){const m=out.match(/data-ct361probe="([^"]*)"/);throw new Error('R361_BROWSER '+(m?.[1]||'probe did not finish'))}console.log('R361_BROWSER_OK repeated Trocar/Watchlist/Visto stay clickable after every repaint');
