import {spawnSync} from 'node:child_process';import {readFile} from 'node:fs/promises';import {resolve} from 'node:path';await import('./build-r369.mjs');const g=spawnSync(process.execPath,['test-r369.mjs'],{cwd:process.cwd(),stdio:'inherit',env:{...process.env,CT_R369_SKIP_BUILD:'1'}});if(g.status!==0)throw new Error('r369 static gate failed');const r=JSON.parse(await readFile(resolve('dist/release.json'),'utf8'));if(r.version!=='1.0.160'||r.revision!=='r369-official-1.0.160')throw new Error('r369 identity');console.log('WEB_R369_OFFICIAL_OK');