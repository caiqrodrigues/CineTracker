/* CineTracker Web 1.0.49 r258 — account-ground-truth Home + resilient Discover + native mobile horizontal scroll. WEB ONLY. */
(()=>{
'use strict';
if(window.__ctR258)return;
window.__ctR258='account-ground-truth-home-discover-native-scroll';
window.__ctR258Home='payload-pending-first+raw-smackdown-priority-sequence+historic-holes-ignored';
window.__ctR258Discover='safe-item-hydration+strict-personal-exclusions+cached-complete-pools';
window.__ctR258Horizontal='native-pan-x+mouse-drag+discover-and-details';
window.__ctR258Frozen='sports-profile-configs-r257-unchanged';

const q258=(s,r=document)=>r?.querySelector?.(s)||null;
const qa258=(s,r=document)=>[...(r?.querySelectorAll?.(s)||[])];
const n258=v=>{const n=Number(v);return Number.isFinite(n)?n:0};
const norm258=v=>String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const esc258=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]||c));
const DAY258=86400000;
function day258(d=new Date()){try{return new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).format(d)}catch{return d.toISOString().slice(0,10)}}
function shift258(n){const d=new Date();d.setHours(12,0,0,0);d.setDate(d.getDate()+n);return day258(d)}
const timeout258=(p,ms,fallback=null)=>Promise.race([Promise.resolve(p),new Promise(r=>setTimeout(()=>r(fallback),ms))]);
const title258=x=>x?.media_title||x?.title||x?.name||x?.raw_tmdb?.title||x?.raw_tmdb?.name||'Sem título';
const type258=x=>String(x?.media_type||x?.type||x?.raw_tmdb?.media_type||'tv')==='movie'?'movie':'tv';
const id258=x=>n258(x?.tmdb_id||x?.source_tmdb_id||x?.raw_tmdb?.source_tmdb_id||x?.raw_tmdb?.id||x?.id);
const poster258=x=>x?.poster_path||x?.raw_tmdb?.poster_path||null;
const year258=x=>String(x?.release_year||x?.release_date||x?.first_air_date||x?.raw_tmdb?.release_date||x?.raw_tmdb?.first_air_date||'').slice(0,4);
const score258=x=>n258(x?.vote_average??x?.raw_tmdb?.vote_average);
const key258=x=>`${type258(x)}:${id258(x)}`;
const epPos258=(s,e)=>n258(s)>0&&n258(e)>0?n258(s)*100000+n258(e):0;
const WEEKLY258=/(^|\s)raw(\s|$)|smackdown/i;
const weekly258=x=>WEEKLY258.test(norm258(title258(x)));
const GENRES258={28:'Ação',12:'Aventura',16:'Animação',35:'Comédia',80:'Crime',99:'Documentário',18:'Drama',10751:'Família',14:'Fantasia',36:'História',27:'Terror',10402:'Música',9648:'Mistério',10749:'Romance',878:'Ficção científica',10770:'Cinema TV',53:'Thriller',10752:'Guerra',37:'Faroeste',10759:'Ação e aventura',10762:'Infantil',10763:'Notícias',10764:'Reality',10765:'Sci-Fi e fantasia',10766:'Novela',10767:'Talk show',10768:'Guerra e política'};
function genres258(x,limit=3){const a=Array.isArray(x?.genres)?x.genres.map(g=>typeof g==='string'?g:g?.name).filter(Boolean):Array.isArray(x?.raw_tmdb?.genres)?x.raw_tmdb.genres.map(g=>typeof g==='string'?g:g?.name).filter(Boolean):[];if(a.length)return a.slice(0,limit);return [...new Set((x?.genre_ids||x?.raw_tmdb?.genre_ids||[]).map(v=>GENRES258[n258(v)]).filter(Boolean))].slice(0,limit)}
function img258(p,size='w342'){try{return typeof img==='function'?img(p,size):p||''}catch{return p||''}}
async function tmdb258(path,params={}){try{return await tmdb(path,params)}catch{return null}}

/* HOME ------------------------------------------------------------------
   The user video proves two different failures:
   1) ordinary shows already carry a real released-vs-watched deficit but remain in Em dia;
   2) Raw/SmackDown use the first historical hole as next episode.
   Ordinary payload-known deficits are fixed synchronously, before paint. Raw/SmackDown are
   audited first and only against aired episodes strictly after the highest watched frontier. */
function nextFromRow258(r){const n=r?.next_episode||r?.next_episode_to_watch||r?.next_unwatched_episode||{};const s=n258(r?.next_season_number??n?.season_number),e=n258(r?.next_episode_number??n?.episode_number);return s>0&&e>0?{season_number:s,episode_number:e,name:r?.next_episode_title||n?.name||'',air_date:r?.next_episode_air_date||n?.air_date||'',vote_average:n258(n?.vote_average)}:null}
function releasedMissing258(r){return Math.max(n258(r?.history_missing_episodes),n258(r?.released_episodes)-n258(r?.watched_episodes),n258(r?.missing_episodes));}
function recent258(ds){const t=new Date(`${String(ds||'').slice(0,10)}T12:00:00`).getTime();return Number.isFinite(t)&&Date.now()-t<=30*DAY258}
function payloadNormalize258(r){if(!r||weekly258(r)||n258(r?.watched_episodes)<=0)return r;const missing=releasedMissing258(r),next=nextFromRow258(r),bucket=String(r?.home_bucket||'');if(missing>0&&(bucket==='up_to_date'||r?.is_caught_up===true)){r.home_bucket=(recent258(next?.air_date)||recent258(r?.last_watched_at))?'continue':'dust';r.is_caught_up=false;r.history_missing_episodes=missing;r._ct258DisplayMissing=missing;if(next)r._ct258Next=next}return r}
function normalizeHomePayload258(){for(const r of homeCache?.series||[])payloadNormalize258(r)}
function watchedSet258(state){const set=new Set();for(const e of Array.isArray(state?.episodes)?state.episodes:[]){const p=epPos258(e?.season_number,e?.episode_number);if(p)set.add(p)}return set}
function frontier258(state){let p=0;for(const v of watchedSet258(state))p=Math.max(p,v);return p}
function epLabel258(e){return e?`S${String(n258(e.season_number)).padStart(2,'0')}E${String(n258(e.episode_number)).padStart(2,'0')}`:''}
async function weeklySequence258(r){const id=id258(r);if(!id)return null;const [state,detail]=await Promise.all([timeout258(rpc('cinetracker_series_episode_state_v1',{p_tmdb_id:id,p_today:day258()}),5500,null),timeout258(tmdb258(`/tv/${id}`),5500,null)]);if(!state||!detail)return null;const watched=watchedSet258(state),frontier=frontier258(state);if(!frontier)return null;const fromSeason=Math.floor(frontier/100000),last=detail?.last_episode_to_air,lastSeason=Math.max(fromSeason,n258(last?.season_number));const seasons=[];for(let s=fromSeason;s<=lastSeason;s++)seasons.push(s);const seasonData=await Promise.all(seasons.map(s=>timeout258(tmdb258(`/tv/${id}/season/${s}`),5500,null)));const pending=[];for(let i=0;i<seasons.length;i++){const s=seasons[i],d=seasonData[i];for(const e of d?.episodes||[]){const sn=n258(e?.season_number||s),en=n258(e?.episode_number),p=epPos258(sn,en),ds=String(e?.air_date||'').slice(0,10);if(p>frontier&&ds&&ds<=day258()&&!watched.has(p))pending.push({...e,season_number:sn,episode_number:en,_p:p})}}pending.sort((a,b)=>a._p-b._p);return{frontier,pending,next:pending[0]||null,detail}}
function applyWeekly258(r,seq){if(!r||!seq)return false;const before=`${r.home_bucket}|${r.next_season_number}|${r.next_episode_number}|${r.history_missing_episodes}`;const next=seq.next,missing=seq.pending.length;r._ct258WeeklyResolved=true;r._ct258DisplayMissing=missing;r._ct258Next=next||null;r.history_missing_episodes=missing;if(next){r.home_bucket=recent258(next.air_date)?'continue':'dust';r.is_caught_up=false;r.next_season_number=n258(next.season_number);r.next_episode_number=n258(next.episode_number);r.next_episode_title=next.name||'';r.next_episode_air_date=next.air_date||'';r.next_episode={season_number:n258(next.season_number),episode_number:n258(next.episode_number),name:next.name||'',air_date:next.air_date||'',vote_average:n258(next.vote_average)};r.next_episode_to_watch=r.next_episode;r.next_unwatched_episode=r.next_episode;r.released_episodes=n258(r.watched_episodes)+missing}else{r.home_bucket='up_to_date';r.is_caught_up=true;r.next_season_number=null;r.next_episode_number=null;r.next_episode_title='';r.next_episode_air_date='';r.next_episode=null;r.next_episode_to_watch=null;r.next_unwatched_episode=null;r.released_episodes=n258(r.watched_episodes)}return before!==`${r.home_bucket}|${r.next_season_number}|${r.next_episode_number}|${r.history_missing_episodes}`}
let weeklyToken258=0;
async function auditWeeklyFirst258(){const token=++weeklyToken258,rows=(homeCache?.series||[]).filter(r=>weekly258(r)&&n258(r?.watched_episodes)>0&&id258(r)>0);let changed=false;await Promise.all(rows.map(async r=>{try{const seq=await weeklySequence258(r);if(token!==weeklyToken258)return;if(seq&&applyWeekly258(r,seq))changed=true}catch(_){}}));if(token!==weeklyToken258||route()!=='home')return;if(changed&&typeof paintHome==='function')paintHome();repairHomeDom258()}
function section258(root,name){const n=norm258(name);return qa258('section.home-section,section',root).find(s=>norm258(q258('.panel-head h3,.panel-head h2,h3,h2',s)?.textContent||'')===n)||null}
function rowEl258(root,r){const id=id258(r);if(id){const hit=q258(`[data-media="tv:${id}"]`,root);if(hit)return hit.closest?.('.media-row,.home-card,article,li,.card')||hit}const needle=norm258(title258(r));if(!needle)return null;for(const el of qa258('.stack>* ,.media-row,.home-card,article',root)){if(norm258(el.textContent||'').includes(needle))return el}return null}
function targetName258(bucket){return bucket==='continue'?'Assistir a seguir':bucket==='dust'?'Juntando poeira':bucket==='up_to_date'?'Em dia':bucket==='completed'?'Concluídas':bucket==='not_started'?'Não iniciadas / Watchlist':''}
function meta258(r){const watched=n258(r?.watched_episodes),missing=r?._ct258DisplayMissing!=null?n258(r._ct258DisplayMissing):releasedMissing258(r),next=r?._ct258Next||nextFromRow258(r),label=epLabel258(next);if(weekly258(r)&&r?._ct258WeeklyResolved){const relevant=watched+missing;return `${watched}/${relevant} · ${missing>0?`Faltam ${missing}${label?` · próximo ${label}`:''}`:'Em dia'}`}const total=Math.max(n258(r?.total_episodes),n258(r?.released_episodes),watched);return `${watched}/${total||'?'} · ${missing>0?`Faltam ${missing}${label?` · próximo ${label}`:''}`:'Em dia'}`}
function repairRowText258(el,r){if(!el)return;const next=r?._ct258Next||nextFromRow258(r),m=meta258(r);const smalls=qa258('small',el);if(smalls.length)smalls[0].textContent=m;const label=epLabel258(next),line=next?`Próximo: ${next.name||label}${n258(next.vote_average)?`  ★ ${n258(next.vote_average).toFixed(1)}`:''}${next.air_date?`  ${new Date(`${next.air_date}T12:00:00`).toLocaleDateString('pt-BR')}`:''}`:'Ep: Em dia';const cand=qa258('span,p,div,strong,b',el).filter(x=>x.children.length<=2&&x!==el).find(x=>/^(pr[oó]ximo|ep):/i.test(String(x.textContent||'').trim()));if(cand)cand.textContent=line}
function updateSectionCounts258(root){for(const s of qa258('section.home-section',root)){const st=q258('.stack',s),c=q258('.panel-head small',s);if(!st||!c)continue;const rows=[...st.children].filter(x=>!x.classList?.contains('empty'));c.textContent=String(rows.length);const emp=q258('.empty',st);if(rows.length&&emp)emp.remove()}}
function repairHomeDom258(){const root=q258('[data-home]');if(!root)return;normalizeHomePayload258();for(const r of homeCache?.series||[]){const el=rowEl258(root,r),name=targetName258(String(r?.home_bucket||''));if(!el||!name)continue;const sec=section258(root,name),stack=sec&&q258('.stack',sec);if(stack&&el.parentElement!==stack)stack.appendChild(el);if(r?._ct258DisplayMissing!=null||r?._ct258WeeklyResolved||((r?.is_caught_up===false)&&releasedMissing258(r)>0))repairRowText258(el,r)}updateSectionCounts258(root)}
const basePaintHome258=typeof paintHome==='function'?paintHome:null;if(basePaintHome258)paintHome=function(){normalizeHomePayload258();const out=basePaintHome258.apply(this,arguments);repairHomeDom258();return out};
const baseRenderHome258=renderHome;renderHome=async function(seq){normalizeHomePayload258();const out=await baseRenderHome258(seq);if(seq===navSeq&&route()==='home'){normalizeHomePayload258();repairHomeDom258();void auditWeeklyFirst258()}return out};

/* DISCOVER --------------------------------------------------------------
   A failed Watchlist hydration must never reject the whole Pra você page. Every TMDB call is
   isolated. Personal state is loaded before recommendation candidates and exclusions apply to
   all recommendation/public pools; Watchlist is allowed only inside its own block. */
const DTABS258=[['foryou','Pra você'],['top10','Top 10'],['trending','Em alta'],['popular','Populares'],['new','Novidades'],['releases','Lançamentos'],['anticipated','Mais Aguardados'],['top','Mais bem avaliados'],['calendar','Calendário']];
const discover258={tab:'foryou',type:'all',gen:0,known:null,knownAt:0,cache:new Map()};
function card258(x){const type=type258(x),id=id258(x),p=poster258(x),yr=year258(x),gs=genres258(x),sc=score258(x);return `<article class="ct255-media-card ct258-media-card"><button type="button" data-media="${type}:${id}">${p?`<img class="ct255-media-poster ct258-media-poster" src="${esc258(img258(p,'w342'))}" alt="" loading="lazy">`:'<div class="ct255-media-poster ct258-media-poster ct255-poster-empty">Sem capa</div>'}<div class="ct255-media-copy"><b>${esc258(title258(x))}</b><small>${[yr,gs.join(' · ')].filter(Boolean).join(' · ')||'—'}</small><span>${sc?`★ ${sc.toFixed(1)}`:'Sem nota'}</span></div></button></article>`}
function rail258(rows){return `<div class="ct258-media-rail ct258-native-x">${(rows||[]).map(card258).join('')||'<div class="empty">Nenhum item elegível no momento.</div>'}</div>`}
async function known258(force=false){if(!force&&discover258.known&&Date.now()-discover258.knownAt<120000)return discover258.known;const fail=Symbol('fail'),[dash,wl,ni]=await Promise.all([timeout258(rpc('cinetracker_profile_media_dashboard_v0991',{}),6500,fail),timeout258(rpc('cinetracker_watchlist_full_v119',{}),6500,fail),typeof api==='function'?timeout258(api('media_overrides?select=media_id,state&state=eq.NotInterested'),4500,[]):Promise.resolve([])]);if(dash===fail||wl===fail)throw new Error('Não foi possível validar sua biblioteca pessoal.');const rows=Array.isArray(dash)?dash:[],watchRows=Array.isArray(wl?.rows)?wl.rows:[],blockedIds=new Set((Array.isArray(ni)?ni:[]).map(x=>n258(x?.media_id)).filter(Boolean)),seen=new Set(),watch=new Set(),following=new Set(),blocked=new Set(),excluded=new Set();for(const x of rows){const k=key258(x);if(!id258(x))continue;const isSeen=!!(x?.is_seen||x?.is_completed||n258(x?.watched_episodes)>0),isWatch=!!(x?.is_watchlist||x?.is_added_to_watchlist||x?.is_watch_later),isFollowing=!!(x?.is_in_progress||x?.is_up_to_date||x?.is_completed),isBlocked=!!(x?.is_not_interested||blockedIds.has(n258(x?.media_id)));if(isSeen)seen.add(k);if(isWatch)watch.add(k);if(isFollowing)following.add(k);if(isBlocked)blocked.add(k);if(isSeen||isWatch||isFollowing||isBlocked)excluded.add(k)}for(const x of watchRows){const k=key258(x);if(id258(x)){watch.add(k);excluded.add(k)}}const out={rows,watchRows,seen,watch,following,blocked,excluded,ready:true};discover258.known=out;discover258.knownAt=Date.now();return out}
function strict258(x,k,{allowWatch=false,fresh=false}={}){const id=id258(x),yr=n258(year258(x)),sc=score258(x),t=norm258(title258(x));if(!id||!poster258(x)||sc<7.5||yr<=1990)return false;if(/wwe|raw|smackdown/.test(t))return false;const gs=genres258(x,8).map(norm258);if(gs.length&&gs.every(g=>g==='drama'||g==='documentario'))return false;const kk=key258(x);if(k.seen.has(kk)||k.following.has(kk)||k.blocked.has(kk)||(!allowWatch&&k.watch.has(kk)))return false;if(fresh){const ds=String(x?.release_date||x?.first_air_date||'').slice(0,10);if(!ds||ds<shift258(-30)||ds>day258())return false}return true}
function browse258(x,k){return !!(id258(x)&&poster258(x)&&!k.excluded.has(key258(x)))}
async function list258(path,params={},kind=null,pages=3){const jobs=[];for(let page=1;page<=pages;page++)jobs.push(timeout258(tmdb258(path,{...params,page}),5500,null));const settled=await Promise.all(jobs),all=[];for(const d of settled)for(const x of d?.results||[])all.push({...x,media_type:kind||x.media_type||type258(x)});const out=[],seen=new Set();for(const x of all){if(!['movie','tv'].includes(type258(x)))continue;const k=key258(x);if(!id258(x)||seen.has(k))continue;seen.add(k);out.push(x)}return out}
async function hydrate258(x){if(!id258(x))return null;if(poster258(x)&&year258(x)&&score258(x))return x;const t=type258(x),d=await timeout258(tmdb258(`/${t}/${id258(x)}`),4500,null);return d?{...x,...d,media_type:t,raw_tmdb:{...(x?.raw_tmdb||{}),...d}}:x}
async function forYou258(force=false){const k=await known258(force),[trendM,trendT,freshM,freshT]=await Promise.all([list258('/trending/movie/day',{},'movie',1),list258('/trending/tv/day',{},'tv',1),list258('/discover/movie',{'primary_release_date.gte':shift258(-30),'primary_release_date.lte':day258(),sort_by:'vote_average.desc','vote_count.gte':25},'movie',2),list258('/discover/tv',{'first_air_date.gte':shift258(-30),'first_air_date.lte':day258(),sort_by:'vote_average.desc','vote_count.gte':20},'tv',2)]);let daily=[...trendM,...trendT].filter(x=>strict258(x,k)).slice(0,1);if(!daily.length){const fallback=[...(await list258('/movie/top_rated',{},'movie',1)),...(await list258('/tv/top_rated',{},'tv',1))];daily=fallback.filter(x=>strict258(x,k)).slice(0,1)}const byKey=new Map(k.rows.map(x=>[key258(x),x])),candidates=[];for(const x of k.watchRows){const kk=key258(x),d=byKey.get(kk);if(!id258(x)||k.seen.has(kk)||k.following.has(kk)||k.blocked.has(kk)||d?.is_completed)continue;if(!candidates.some(y=>key258(y)===kk))candidates.push({...x,media_type:type258(x)})}const hydrated=await Promise.all(candidates.slice(0,24).map(hydrate258)),watch=hydrated.filter(x=>x&&poster258(x)).slice(0,12),fresh=[...freshM,...freshT].filter(x=>strict258(x,k,{fresh:true})).slice(0,24);return{__ct258ForYou:true,daily,watchlist:watch,fresh}}
async function browseRows258(tab){const k=await known258(false),ok=x=>browse258(x,k);let rows=[];if(tab==='top10')rows=await list258('/trending/all/day',{},null,3);else if(tab==='trending')rows=await list258('/trending/all/week',{},null,4);else if(tab==='popular')rows=[...(await list258('/movie/popular',{},'movie',3)),...(await list258('/tv/popular',{},'tv',3))].sort((a,b)=>n258(b.popularity)-n258(a.popularity));else if(tab==='new')rows=[...(await list258('/discover/movie',{'primary_release_date.gte':shift258(-30),'primary_release_date.lte':day258(),sort_by:'primary_release_date.desc'},'movie',3)),...(await list258('/discover/tv',{'first_air_date.gte':shift258(-30),'first_air_date.lte':day258(),sort_by:'first_air_date.desc'},'tv',3))];else if(tab==='releases')rows=[...(await list258('/discover/movie',{'primary_release_date.gte':shift258(-7),'primary_release_date.lte':shift258(30),sort_by:'primary_release_date.asc'},'movie',3)),...(await list258('/discover/tv',{'first_air_date.gte':shift258(-7),'first_air_date.lte':shift258(30),sort_by:'first_air_date.asc'},'tv',3))];else if(tab==='anticipated')rows=[...(await list258('/discover/movie',{'primary_release_date.gte':shift258(1),sort_by:'popularity.desc'},'movie',3)),...(await list258('/discover/tv',{'first_air_date.gte':shift258(1),sort_by:'popularity.desc'},'tv',3))];else rows=[...(await list258('/movie/top_rated',{},'movie',3)),...(await list258('/tv/top_rated',{},'tv',3))].sort((a,b)=>score258(b)-score258(a));const out=[],seen=new Set();for(const x of rows){if(!ok(x))continue;const kk=key258(x);if(seen.has(kk))continue;seen.add(kk);out.push(x)}return tab==='top10'?out.slice(0,10):out.slice(0,80)}
async function rows258(tab,force=false){const ck=`${tab}:${day258()}`,old=discover258.cache.get(ck);if(!force&&old&&Date.now()-old.at<120000)return old.data;let data;if(tab==='foryou')data=await forYou258(force);else if(tab==='calendar')data=await rpc('cinetracker_calendar_watchlist_v0997',{p_from:day258(),p_to:shift258(90)}).catch(()=>[]);else data=await browseRows258(tab);discover258.cache.set(ck,{at:Date.now(),data});return data}
function block258(title,rows,refresh=false){return `<section class="panel ct258-discover-block"><div class="panel-head"><h2>${esc258(title)}</h2>${refresh?'<button type="button" class="chip" data-ct258-refresh>Trocar</button>':`<small>${rows.length}</small>`}</div>${rail258(rows)}</section>`}
function paintDiscover258(rows,tab=discover258.tab){const h=q258('[data-ct258-discover-content]');if(!h)return;if(rows?.__ct258ForYou){h.innerHTML=`<div class="page" data-ct258-foryou>${block258('Indicação do Dia',rows.daily||[],true)}${block258('Da sua Watchlist',rows.watchlist||[])}${block258('100% Novos',rows.fresh||[])}</div>`;markRails258(h);return}let a=Array.isArray(rows)?rows:[];if(discover258.type!=='all')a=a.filter(x=>discover258.type==='movie'?type258(x)==='movie':type258(x)==='tv');if(tab==='calendar'){const groups=new Map();for(const x of a){const ds=String(x.calendar_date||x.release_date||x.first_air_date||'').slice(0,10)||'Sem data';if(!groups.has(ds))groups.set(ds,[]);groups.get(ds).push(x)}h.innerHTML=`<div class="page">${[...groups.entries()].map(([d,g])=>block258(d==='Sem data'?d:new Date(`${d}T12:00:00`).toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'long'}),g)).join('')||'<div class="empty">Nenhum lançamento da sua Watchlist neste período.</div>'}</div>`}else h.innerHTML=rail258(a);markRails258(h)}
function syncDiscover258(){qa258('[data-ct258-discover-tab]').forEach(b=>b.classList.toggle('active',b.dataset.ct258DiscoverTab===discover258.tab));qa258('[data-ct258-discover-type]').forEach(b=>b.classList.toggle('active',b.dataset.ct258DiscoverType===discover258.type))}
async function loadDiscover258(tab=discover258.tab,force=false){const gen=++discover258.gen;discover258.tab=tab;syncDiscover258();const h=q258('[data-ct258-discover-content]'),ck=`${tab}:${day258()}`,cached=discover258.cache.get(ck);if(!force&&cached&&Date.now()-cached.at<120000){paintDiscover258(cached.data,tab);return}if(h&&!h.children.length)h.innerHTML='<div class="ct258-skeleton">Carregando títulos…</div>';try{const r=await rows258(tab,force);if(gen!==discover258.gen||route()!=='discover')return;paintDiscover258(r,tab)}catch(e){if(gen!==discover258.gen)return;if(h)h.innerHTML=`<div class="empty">${esc258(e?.message||'Não foi possível carregar o Descobrir agora.')}</div>`}}
renderDiscover=async function(seq){setApp(shell('Descobrir','Recomendações, tendências, novidades e calendário.','discover',`<div class="page" data-discover data-ct258-discover><div class="tabs ct258-discover-tabs ct258-native-x">${DTABS258.map(([k,l])=>`<button type="button" class="chip ${discover258.tab===k?'active':''}" data-ct258-discover-tab="${k}">${l}</button>`).join('')}</div><div class="filters ct258-discover-types ct258-native-x">${[['all','Todos'],['movie','Filmes'],['tv','Séries']].map(([k,l])=>`<button type="button" class="chip ${discover258.type===k?'active':''}" data-ct258-discover-type="${k}">${l}</button>`).join('')}</div><div data-ct258-discover-content></div></div>`));if(seq!==navSeq||route()!=='discover')return;markRails258(q258('[data-ct258-discover]'));void loadDiscover258(discover258.tab,false)};

/* HORIZONTAL ------------------------------------------------------------
   Mobile touch uses the browser's native horizontal scroll. The r257 pointer capture is
   disabled for touch by the r258 build. Mouse/pen keep drag-to-scroll. */
const RAIL258='.ct258-native-x,.ct258-media-rail,.ct255-media-rail,.season-tabs,.season-list,.season-row,[data-seasons],.ct169-season-row,.ct169-season-chart-carousel,.ct169-chart-scroll,.ct244-seasons-scroll,.ct244-chart-scroll,.episode-list,.episodes-list,.episodes-row,.episode-row,[data-episodes],[data-season-episodes],.related-scroll,.related-grid,.related-row,[data-related],.similar-scroll,.similar-grid,.similar-row,[data-similar],.cast-scroll,.cast-grid,.cast-row,[data-cast],.actors-scroll,.actors-grid,.actors-row,[data-actors],.people-scroll,.people-grid,.people-row,[data-people],.episode-graph,.graph-shell,.chart-wrap,.chart-scroll,[data-chart],[data-episode-chart]';
function bindMouseDrag258(el){if(!el||el.dataset.ct258Drag==='1')return;el.dataset.ct258Drag='1';el.classList.add('ct258-native-x');let st=null;el.addEventListener('pointerdown',e=>{if(e.pointerType==='touch')return;if(e.button!=null&&e.button!==0)return;if(el.scrollWidth<=el.clientWidth+2)return;st={id:e.pointerId,x:e.clientX,left:el.scrollLeft};try{el.setPointerCapture(e.pointerId)}catch(_){}});el.addEventListener('pointermove',e=>{if(!st||e.pointerId!==st.id)return;const dx=e.clientX-st.x;if(Math.abs(dx)<3)return;e.preventDefault();el.scrollLeft=st.left-dx});const end=e=>{if(st&&e.pointerId===st.id)st=null};el.addEventListener('pointerup',end);el.addEventListener('pointercancel',end);el.addEventListener('lostpointercapture',()=>{st=null})}
function semanticRails258(root=document){for(const section of qa258('section,.panel,[data-series-detail],[data-movie-detail]',root)){const head=norm258(q258('h2,h3,h4,.section-title,.eyebrow',section)?.textContent||'');if(!/(temporad|episod|melhores|piores|grafico|nota|relacionad|semelhant|atores|elenco|cast)/.test(head))continue;let best=null,overflow=0;for(const el of qa258('div,ul,ol',section).slice(0,120)){if(el.children.length<2)continue;let d=0;try{d=el.scrollWidth-el.clientWidth}catch(_){}if(d>overflow){overflow=d;best=el}}if(best&&overflow>4)bindMouseDrag258(best)}}
function markRails258(root=document){const scope=root?.querySelectorAll?root:document;for(const el of qa258(RAIL258,scope))bindMouseDrag258(el);if(root?.matches?.(RAIL258))bindMouseDrag258(root);semanticRails258(scope)}
const app258=q258('#app');let raf258=0;if(app258&&window.MutationObserver){try{new MutationObserver(()=>{cancelAnimationFrame(raf258);raf258=requestAnimationFrame(()=>markRails258(app258))}).observe(app258,{subtree:true,childList:true});window.__ctR258RailObserverActive=true}catch(_){}}
window.addEventListener('resize',()=>requestAnimationFrame(()=>markRails258(app258||document)));
document.addEventListener('click',e=>{const d=e.target?.closest?.('[data-ct258-discover-tab]');if(d){e.preventDefault();e.stopImmediatePropagation();discover258.tab=d.dataset.ct258DiscoverTab;void loadDiscover258(discover258.tab,false);return}const ty=e.target?.closest?.('[data-ct258-discover-type]');if(ty){e.preventDefault();e.stopImmediatePropagation();discover258.type=ty.dataset.ct258DiscoverType;syncDiscover258();const c=discover258.cache.get(`${discover258.tab}:${day258()}`);if(c)paintDiscover258(c.data,discover258.tab);else void loadDiscover258(discover258.tab,false);return}const rf=e.target?.closest?.('[data-ct258-refresh]');if(rf){e.preventDefault();e.stopImmediatePropagation();rf.disabled=true;void loadDiscover258('foryou',true).finally(()=>rf.disabled=false)}},true);
document.addEventListener('cinetracker:data-changed',()=>{discover258.known=null;discover258.knownAt=0;discover258.cache.clear();weeklyToken258++;});
queueMicrotask(()=>{normalizeHomePayload258();repairHomeDom258();markRails258(app258||document)});
window.__ctR258Test={epPos258,payloadNormalize258,releasedMissing258,watchedSet258,frontier258,applyWeekly258,strict258,browse258,markRails258,bindMouseDrag258,hydrate258};
})();
