import {spawnSync} from 'node:child_process';import {readFile} from 'node:fs/promises';import {resolve} from 'node:path';
await import('./build-r378.mjs');const g=spawnSync(process.execPath,['test-r378.mjs'],{cwd:process.cwd(),stdio:'inherit',env:{...process.env,CT_R378_SKIP_BUILD:'1'}});if(g.status!==0)throw new Error('r378 static gate failed');const r=JSON.parse(await readFile(resolve('dist/release.json'),'utf8'));if(r.version!=='1.0.169'||r.revision!=='r378-official-1.0.169')throw new Error('r378 identity');console.log('WEB_R378_OFFICIAL_OK');