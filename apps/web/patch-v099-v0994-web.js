(() => {
'use strict';
if (window.__ct0994WebLoaded) return;
window.__ct0994WebLoaded = true;
window.__ctWebBuild = '0.99.4';
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const img=(p,size='w342')=>p?`${SUPABASE_URL}/functions/v1/tmdb-image?path=${encodeURIComponent(p)}&size=${size}`:'';
const fmt=min=>{min=Math.max(0,Number(min||0));const d=Math.floor(min/1440),h=Math.floor((min%1440)/60),m=Math.round(min%60);return [d?`${d}d`:null,h?`${h}h`:null,m||(!d&&!h)?`${m}min`:null].filter(Boolean).join(' ')};
let payload994=null,busy994=null,tab994='series',renderSeq994=0;
async function load994(force=false){if(payload994&&!force)return payload994;if(busy994)return busy994;busy994=(async()=>{const p=await sbRpc('cinetracker_profile_home_payload_v0994',{});payload994=p||{series:[],movie_watchlist:[],history_episodes:[],history_movies:[]};return payload994})().finally(()=>busy994=null);return busy994}
function rowSeries(x){const rel=Number(x.released_episodes||0),seen=Number(x.watched_episodes||0),missing=Math.max(0,rel-seen),last=x.last_watched_at?new Date(x.last_watched_at).toLocaleDateString('pt-BR'):'Sem atividade';return `<div class="ct992-row" data-ct994-open="${x.media_id}"><div class="ct992-poster"${x.poster_path?` style="background-image:url('${img(x.poster_path)}')"`:''}></div><div class="ct992-info"><div class="ct992-title">${esc(x.title)}</div><div class="ct992-meta">${seen}/${rel||'?'} · Faltam ${missing}</div><div class="ct992-sub">${esc(last)}</div></div></div>`}
function rowMovie(x){const runtime=Number(x.runtime_minutes||0);return `<div class="ct992-row" data-ct994-open="${x.media_id}"><div class="ct992-poster"${x.poster_path?` style="background-image:url('${img(x.poster_path)}')"`:''}></div><div class="ct992-info"><div class="ct992-title">${esc(x.title)}</div><div class="ct992-meta">${x.release_year||'—'}${runtime?` · ${runtime} min`:''}</div><div class="ct992-sub">${esc(x.overview||'Metadados sendo atualizados.')}</div></div><button class="ct992-check" data-ct994-mark-movie="${x.media_id}" title="Marcar como visto">✓</button></div>`}
function histEpisode(h){return `<div class="ct992-row"><div class="ct992-poster"${h.poster_path?` style="background-image:url('${img(h.poster_path)}')"`:''}></div><div class="ct992-info"><div class="ct992-title">${esc(h.media_title||h.title||'Série')}</div><div class="ct992-meta">S${String(h.season_number??0).padStart(2,'0')} E${String(h.episode_number??0).padStart(2,'0')}</div><div class="ct992-sub">${h.watched_at?new Date(h.watched_at).toLocaleString('pt-BR'):''}</div></div><span class="ct992-badge">✓</span></div>`}
function histMovie(h){return `<div class="ct992-row"><div class="ct992-poster"${h.poster_path?` style="background-image:url('${img(h.poster_path)}')"`:''}></div><div class="ct992-info"><div class="ct992-title">${esc(h.media_title||h.title||'Filme')}</div><div class="ct992-meta">${Number(h.plays||1)>1?`x${Number(h.plays)}`:'Visto'}</div><div class="ct992-sub">${h.watched_at?new Date(h.watched_at).toLocaleString('pt-BR'):''}</div></div><span class="ct992-badge">✓</span></div>`}
function section(title,rows,renderer){return `<section class="ct992-section ct992-section-anchor"><div class="ct992-head"><h3>${title}</h3><span class="ct992-count">${rows.length.toLocaleString('pt-BR')}</span></div><div class="ct992-stack">${rows.length?rows.map(renderer).join(''):'<div class="ct992-empty">Nenhum item nesta seção.</div>'}</div></section>`}
function buckets(p){const s=Array.isArray(p.series)?p.series:[];return {cont:s.filter(x=>x.home_bucket==='continue'),dust:s.filter(x=>x.home_bucket==='dust'),up:s.filter(x=>x.home_bucket==='up_to_date'),watch:s.filter(x=>x.home_bucket==='not_started'),done:s.filter(x=>x.home_bucket==='completed')}}
function bind994(root){$$('[data-ct994-tab]',root).forEach(b=>b.onclick=async e=>{e.preventDefault();e.stopPropagation();tab994=b.dataset.ct994Tab;await renderHome994(false)});$$('[data-ct994-open]',root).forEach(el=>el.onclick=e=>{if(e.target.closest('[data-ct994-mark-movie]'))return;const id=Number(el.dataset.ct994Open),all=[...(payload994?.series||[]),...(payload994?.movie_watchlist||[])],row=all.find(x=>Number(x.media_id)===id);if(!row)return;const tmdb=Number(row.tmdb_id||0);if(tmdb>0&&(window.ct92OpenMedia||window.ct91OpenMedia)){(window.ct92OpenMedia||window.ct91OpenMedia)(row.media_type||'movie',tmdb)}});$$('[data-ct994-mark-movie]',root).forEach(b=>b.onclick=async e=>{e.preventDefault();e.stopPropagation();b.disabled=true;try{const id=Number(b.dataset.ct994MarkMovie),row=(payload994?.movie_watchlist||[]).find(x=>Number(x.media_id)===id);await sbRpc('cinetracker_mark_watch_v0994',{p_media_id:id,p_item_type:'movie',p_season_number:null,p_episode_number:null,p_title:row?.title||null,p_runtime_minutes:Number(row?.runtime_minutes||0)||null,p_released_episodes:null,p_watched_at:new Date().toISOString()});await renderHome994(true);window.dispatchEvent(new CustomEvent('cinetracker:data-changed',{detail:{source:'web-0.99.4-movie'}}))}catch(err){console.error('[CineTracker 0.99.4] mark movie',err)}finally{b.disabled=false}})}
async function renderHome994(force=false){const seq=++renderSeq994,root=$('.ct992-shell');if(!root)return false;root.dataset.ct994='loading';try{const p=await load994(force);if(seq!==renderSeq994||!document.body.contains(root))return false;const g=buckets(p);root.innerHTML=`<div class="ct992-tabs"><button class="ct992-tab ${tab994==='series'?'active':''}" data-ct994-tab="series">Séries</button><button class="ct992-tab ${tab994==='movies'?'active':''}" data-ct994-tab="movies">Filmes</button></div><div id="ct994-viewport" class="ct992-viewport">${tab994==='series'?`<div class="ct992-history"><div class="ct992-history-hint">Histórico oculto · puxe para baixo para revelar</div><div class="ct992-stack">${(p.history_episodes||[]).map(histEpisode).join('')||'<div class="ct992-empty">Nenhum episódio no histórico.</div>'}</div></div><div class="ct992-start"><div class="ct992-pull-label">↑ Histórico acima</div>${section('Assistir a seguir',g.cont,rowSeries)}${section('Juntando poeira',g.dust,rowSeries)}${section('Em dia',g.up,rowSeries)}${section('Não Iniciadas / Watchlist',g.watch,rowSeries)}${section('Concluídas',g.done,rowSeries)}</div>`:`<div class="ct992-history"><div class="ct992-history-hint">Vistos ocultos · puxe para baixo para revelar</div><div class="ct992-stack">${(p.history_movies||[]).map(histMovie).join('')||'<div class="ct992-empty">Nenhum filme no histórico.</div>'}</div></div><div class="ct992-start"><div class="ct992-pull-label">↑ Vistos acima</div>${section('Assistir a seguir / Watchlist',p.movie_watchlist||[],rowMovie)}</div>`}</div>`;root.dataset.ct994='ready';bind994(root);requestAnimationFrame(()=>{const vp=$('#ct994-viewport',root),hist=$('.ct992-history',root);if(vp&&hist)vp.scrollTop=hist.offsetHeight});return true}catch(err){console.error('[CineTracker 0.99.4] home',err);root.dataset.ct994='error';return false}}
async function patchProfile994(){const host=$('#ct991-profile');if(!host||host.dataset.ct994Metrics==='loading')return;host.dataset.ct994Metrics='loading';try{const r=await sbRpc('cinetracker_profile_remaining_v0994',{}),m=Array.isArray(r)?r[0]:r;if(!m)return;for(const stat of $$('.ct991-stat',host)){const label=$('.l',stat)?.textContent?.trim();const val=$('.v',stat);if(!val)continue;if(label==='Filmes na Watchlist')val.textContent=Number(m.watchlist_movies||0).toLocaleString('pt-BR');if(label==='Tempo p/ séries em dia')val.textContent=fmt(m.series_remaining_minutes);if(label==='Tempo p/ filmes em dia')val.textContent=fmt(m.watchlist_movie_minutes)}host.dataset.ct994Metrics='ready'}catch(err){host.dataset.ct994Metrics='error';console.error('[CineTracker 0.99.4] profile metrics',err)}}
function nav994(){const defs=[['home','Home'],['discover','Descobrir'],['profile','Perfil'],['settings','Configurações']];$$('.sidebar .nav,.mobile-nav').forEach(nav=>{for(const el of $$('button,a,[data-view],[data-view99],[data-view991]',nav)){const t=String(el.dataset.view||el.dataset.view99||el.dataset.view991||'');if(t==='history'||/histórico/i.test(el.textContent||''))el.remove()}const buttons=$$('button[data-view]',nav);if(buttons.length!==4||buttons.some((b,i)=>b.dataset.view!==defs[i][0])){const mobile=nav.classList.contains('mobile-nav');nav.innerHTML=defs.map(([v,l])=>`<button type="button" data-view="${v}">${mobile?(v==='settings'?'Config.':l):l}</button>`).join('')}})}
function footer994(){const host=$('.content');if(!host)return;$$('.ct993-version,.ct992-version,.ct991-version,.ct99-version,.ct98-version,.ct-version-footer',host).forEach(x=>x.style.display='none');let f=$('.ct994-version',host);if(!f){f=document.createElement('div');f.className='ct994-version';f.style.cssText='text-align:center;color:#6f8798;font-size:11px;margin:28px 0 8px';host.appendChild(f)}f.textContent='CineTracker • v0.99.4'}
let timer=0;function reconcile(){clearTimeout(timer);timer=setTimeout(()=>{nav994();footer994();const home=$('.ct992-shell');if(home&&home.dataset.ct994!=='loading'&&home.dataset.ct994!=='ready')void renderHome994(false);if($('#ct991-profile'))void patchProfile994()},35)}
for(const d of [0,80,220,500,900])setTimeout(reconcile,d);window.addEventListener('cinetracker:data-changed',()=>{payload994=null;reconcile()});setTimeout(()=>{const app=$('#app');if(app)new MutationObserver(reconcile).observe(app,{childList:true,subtree:true})},180);
})();